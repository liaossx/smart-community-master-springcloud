server:
  port: 8087

spring:
  application:
    name: system-service
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.10.101:8848
      config:
        server-addr: 192.168.10.101:8848
        file-extension: yaml
        extension-configs:
          - data-id: core-service.yaml
            group: DEFAULT_GROUP
            refresh: true
